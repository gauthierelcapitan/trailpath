import { gpxDistanceElevation } from './gpx-distance-elevation';

describe('gpxDistanceElevation', () => {
  it('should work', () => {
    expect(gpxDistanceElevation()).toEqual('gpx-distance-elevation');
  });
});
